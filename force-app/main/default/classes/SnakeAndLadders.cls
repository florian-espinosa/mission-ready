public with sharing class SnakeAndLadders {
    public static void startGame() {
        Integer pos = 1;
        Integer dice = 1;
        String marker = '*';
        while (pos < 31) {
            System.debug(marker.repeat(' ',pos));
            
        }
    }
}
